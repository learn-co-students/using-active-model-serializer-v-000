<%= form_for(@post) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, :id => 'title' %><br />
  <%= f.label :description %>
  <%= f.text_area :description, :id => 'description' %><br />
  <%= f.submit "Submit Post" %>
<% end %>
